function fmpModulusEuclidean(a,b,c,d,e){
//e is the quantity of signs in the multipolar system > 1
	let euclideanInterval=Math.sqrt((b**2)+(d**2));
//returns a Real number >=0
return euclideanInterval;

}
